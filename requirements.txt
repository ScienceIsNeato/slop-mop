# Slop-Mop Dependencies
#
# ⚠️  pyproject.toml is the SINGLE SOURCE OF TRUTH for dependencies.
# This file exists as a fallback for setup.sh if editable install fails,
# and as a human-readable summary. Keep it in sync with pyproject.toml.
#
# Install via: pip install -e .  (preferred)
#         or:  pip install -r requirements.txt  (fallback)

# Tools slop-mop checks rely on
vulture>=2.14
ruff>=0.1.0

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
diff-cover>=7.0.0

# Linting and formatting
black>=26.3.1
isort>=5.12.0
autoflake>=2.0.0
flake8>=6.0.0

# Type checking
mypy>=1.17.0
pyright>=1.1.0

# Security scanning
bandit>=1.7.0
detect-secrets>=1.4.0
semgrep>=1.140.0
pip-audit>=2.0.0
cryptography>=46.0.7  # CVE GHSA-r6ph-v2qm-q3c2, GHSA-m959-cc7f-wv43, GHSA-p423-j2cm-9vmq
pyasn1>=0.6.3  # GHSA-jr27-m4p2-rc6r
requests>=2.33.0  # GHSA-gc5v-m9x4-r6x2

# Templating (optional — only needed for the jinja2_templates gate)
# jinja2>=3.0.0   # install via: pip install slopmop[templates]
