# Slop-Mop Dependencies
# This is the single source of truth for tool dependencies.
# Install via: pip install -r requirements.txt (in your project venv)

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0
diff-cover>=7.0.0
jinja2>=3.0.0  # For template validation tests

# Linting and formatting
black>=23.0.0
isort>=5.12.0
autoflake>=2.0.0
flake8>=6.0.0

# Type checking
mypy>=1.0.0
pyright>=1.1.0

# Security scanning
bandit>=1.7.0
detect-secrets>=1.4.0
semgrep>=1.0.0
pip-audit>=2.0.0
cryptography>=46.0.5  # CVE GHSA-r6ph-v2qm-q3c2 fixed in 46.0.5

# Quality analysis
vulture>=2.14
