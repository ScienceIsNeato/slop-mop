# Slop-Mop Dependencies
#
# Generated from pyproject.toml by scripts/generate_requirements_txt.py.
# pyproject.toml is the single source of truth; this file exists as a fallback
# install path and a human-readable summary of the toolchain slop-mop expects.
#
# Install via: pip install -e .  (preferred)
#         or:  pip install -r requirements.txt  (fallback)

# Core runtime
tomli>=1.0.0; python_version < '3.11'

# Static analysis
vulture>=2.14
radon>=5.1.0

# Linting and formatting
black>=26.3.1
isort>=5.12.0
autoflake>=2.0.0
flake8>=6.0.0
ruff>=0.1.0

# Type checking
mypy>=1.17.0
pyright>=1.1.0

# Security scanning
bandit>=1.7.0
detect-secrets>=1.4.0
cryptography>=46.0.7
pyasn1>=0.6.3
requests>=2.33.0
semgrep>=1.140.0
pip-audit>=2.0.0

# Testing
pytest>=9.0.3
pytest-cov>=4.0.0
pytest-testmon>=2.0.0
diff-cover>=7.0.0
jsonschema>=4.0.0

# Templates (optional — only needed for the jinja2_templates gate)
jinja2>=3.0.0
